<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/questionSetItemCardView"
    android:layout_marginTop="0dp"
    android:layout_marginBottom="15dp"
    android:layout_marginLeft="10dp"
    android:layout_marginRight="10dp"
    app:cardBackgroundColor="@color/new_primary_variant"
    app:cardCornerRadius="@dimen/cardview_default_radius"
    >

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="6dp">

        <TextView
            android:id="@+id/questionSetItemTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="6dp"
            android:textSize="18sp"
            android:textColor="@color/new_on_primary"
            android:text="@string/question_set_name_dummy"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintTop_toBottomOf="parent"
            />

        <ImageButton
            android:id="@+id/editQuestionSetStartButton"
            android:layout_width="30dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="@id/questionSetItemTextView"
            app:layout_constraintBottom_toBottomOf="@id/questionSetItemTextView"
            app:layout_constraintRight_toLeftOf="@+id/playQuestionSetButton"
            android:layout_marginEnd="10dp"
            android:src="@drawable/ic_edit"
            android:background="@null"/>

        <ImageButton
            android:id="@+id/playQuestionSetButton"
            android:layout_width="30dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="@id/questionSetItemTextView"
            app:layout_constraintBottom_toBottomOf="@id/questionSetItemTextView"
            app:layout_constraintRight_toRightOf="parent"
            android:src="@drawable/ic_play"
            android:background="@null"/>



    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>